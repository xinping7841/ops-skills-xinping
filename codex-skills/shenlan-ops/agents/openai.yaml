interface:
  display_name: "深澜运维"
  short_description: "深澜现场网络台账、备份监控和配置查询"
  default_prompt: "Use $shenlan-ops to look up Shenlan network inventory, VLANs, device access, backups, or monitoring notes."

policy:
  allow_implicit_invocation: true
