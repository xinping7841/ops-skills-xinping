# node-123 服务器部署状态

更新：2026-06-25

## 硬件

- GPU: NVIDIA RTX 4090D 24GB
- DRAM: 93GB
- CPU: Intel i9-10940X 28T
- Disk: NVMe 457GB (/), Intel U.2 4TB (NTFS, /mnt/intel-u2-4t)
- System: Ubuntu 24.04, NVIDIA 595.71.05, CUDA 13.2

## 运行服务

| 端口 | 服务 | 状态 |
|------|------|------|
| 7860 | 混元3D-2.1 Gradio | ✅ 运行中 |
| 4000 | Smart Model Router | ✅ 运行中 |
| 22 | SSH (sl123) | ✅ |
| 8001 | vLLM Qwen3-14B | ❌ 已停 |
| 8000 | vLLM Qwen3-32B | ❌ 已停 |

## SSH 加固状态

- 2026-06-25 断电重启后验证：`ssh.service` 与 `ssh.socket` 均为 `enabled` + `active`。
- LAN SSH：`ssh node-123-lan` / `ssh node-123` -> `192.168.50.123`，用户 `sl123`。
- Tailscale SSH：`ssh node-123-ts` -> `100.119.214.90`，用户 `sl123`。
- `/home/sl123/.ssh` 权限为 `700`，`authorized_keys` 权限为 `600`，已备份到 `/home/sl123/.ssh/backups/` 并清理 CR 字符。
- 当前可用 key 指纹包含 12700K、`wanghongyu@bogon`、macbook-air nodes 等 ED25519 key；不要随意删除授权 key。

## Python 环境

| 环境 | 路径 | Python | 用途 |
|------|------|--------|------|
| venv-vllm | `/home/sl123/venvs/vllm` | 3.12 | vLLM 0.23.0 (已升级) |
| venv-hunyuan | `/opt/hunyuan3d/hunyuan3d21` | 3.10 | 混元3D-2.1 |

## LLM 模型

| 模型 | 路径 | 大小 |
|------|------|------|
## 2026-06-25 SSH/RDP system hardening

- OpenSSH system drop-in: `/etc/ssh/sshd_config.d/99-codex-hardening.conf`.
- Effective SSH policy: `PasswordAuthentication no`, `KbdInteractiveAuthentication no`, `PermitRootLogin no`, `MaxAuthTries 3`, `X11Forwarding no`, `PubkeyAuthentication yes`, `UsePAM yes`.
- SSH config backup directory: `/etc/ssh/backups-codex-20260625/`.
- RDP owner: `xrdp.service` on TCP/3389, with `xrdp-sesman.service` enabled and active.
- `gnome-remote-desktop.service` is disabled/inactive at system and `sl123` user levels because it previously stole TCP/3389 and denied clients with missing credentials.
- Verified from 12700K: LAN and Tailscale ports 22 and 3389 are reachable; key-only SSH login still works with `ssh node-123-lan`.
- RDP login user should be `sl123`; earlier `LK123` / `lk123` attempts failed because those users do not exist.

| Qwen2.5-32B-Instruct-AWQ | `/mnt/intel-u2-4t/models/vlm/` | 19GB |
| Qwen3-14B-AWQ | `/mnt/intel-u2-4t/models/vlm/` | 9.4GB |
| Qwen3-32B-AWQ | `/mnt/intel-u2-4t/models/vlm/` | 23GB |

## 混元3D-2.1 部署

| 项目 | 值 |
|------|-----|
| 项目路径 | `/opt/hunyuan3d/Hunyuan3D-2.1` |
| 模型路径 | `/data/models/hunyuan3d/` (14GB) |
| 启动脚本 | `/opt/hunyuan3d/start_hunyuan3d_gradio.sh` |
| 日志 | `/var/log/hunyuan3d/gradio.log` |
| 访问地址 | `http://192.168.50.123:7860` |
| GPU 占用 | ~15.6GB |
| Shape 生成 | ✅ 正常 |
| Texture 生成 | ❌ 需编译 DifferentiableRenderer C++ 扩展 |
| 中文 UI | ✅ 已翻译 |

### 混元3D 启动命令

```bash
bash /opt/hunyuan3d/start_hunyuan3d_gradio.sh
```

参数：low_vram_mode, host 0.0.0.0, port 7860

### Texture Pipeline 待修复

- 缺失 `__init__.py` 已补（hy3dpaint, hy3dpaint/src, custom_rasterizer）
- `sys.path` 已修正可导入 DifferentiableRenderer
- `mesh_inpaint_processor.cpp` 需编译

## vLLM (备用)

```bash
# 14B 模式
bash /tmp/start_vllm.sh  # port 8001, 14B AWQ, max-len 32768

# 32B 模式
bash /tmp/start_vllm_32b.sh  # port 8000, 32B AWQ, max-len 4096
```

## SSH

- 用户: sl123
- 密钥: id_ed25519 (12700K), id_ed25519_nodes (集群)
- LAN IP: 192.168.50.123
- Tailscale: 100.119.214.90 (在线，2026-06-25 已验证)
